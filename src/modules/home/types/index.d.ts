export interface ChartData {
  month: string;
  revenue: number;
  orders: number;
}

export interface category {
  category: string;
  percentage: string;
  totalRevenue: number;
  totalOrders: number;
}